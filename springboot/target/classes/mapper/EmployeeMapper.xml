<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    <!--對應mapper名-->
    <mapper namespace="com.example.springboot.mapper.EmployeeMapper">
        <!--id和mappaer方法名字對應，resultType和方法返回類型對應-->
        <select id="selectAll" resultType="com.example.springboot.entity.Employee">
            SELECT * FROM employee
        </select>
        <insert id="insert" parameterType="com.example.springboot.entity.Employee">
            insert into `employee` (name, sex, no, age, description, department_id)
            values (#{name}, #{sex}, #{no}, #{age}, #{description}, #{departmentId})
        </insert>
        <update id="updateById" parameterType="com.example.springboot.entity.Employee">
            update `employee` set name = #{name}, sex = #{sex}, no = #{no},
            age = #{age}, description = #{description}, department_id = #{departmentId}
            where id = #{id}
        </update>
    </mapper>